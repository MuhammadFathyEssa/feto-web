# ── Frontend (Vercel) ──────────────────────────────────────────
# REQUIRED: JWT signing secret — min 32 chars, generate with: openssl rand -base64 48
JWT_SECRET=

# Backend connection — server-side only (NOT exposed to browser)
BACKEND_API_URL=https://feto-agent-production.up.railway.app
BACKEND_API_KEY=

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_KEY=
